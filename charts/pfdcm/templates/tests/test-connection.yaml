apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pfdcm.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "pfdcm.commonLabels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pfdcm.fullname" . }}:{{ .Values.pfdcm.service.port }}']
  restartPolicy: Never
